export type ItemCombobox = {
    id?: number | null
    nombre: string
    descripcion?: string | null
    usuarioRegistro?: string | null
}

export type MenuItem = {
    icon: string,
    title: string,
    path: string,
}